spring:
  datasource:
    url: jdbc:mysql://localhost:3306/doktoridb?serverTimezone=Asia/Seoul&useSSL=false
    username: root
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: false
  flyway:
    enabled: false  # chat 서버는 flyway 비활성화 (api에서만 마이그레이션)

logging:
  level:
    org.hibernate.SQL: debug

app:
  cookie:
    secure: false
    same-site: Lax
