spring:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: false
logging:
  level:
    org.hibernate.SQL: debug
app:
  cookie:
    secure: true
    same-site: None
ai:
  base-url: ${AI_BASE_URL:https://dev.doktori.kr/ai}
  api-key: ${AI_API_KEY}
